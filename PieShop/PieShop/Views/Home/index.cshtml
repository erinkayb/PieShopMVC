@model PieShop.ViewModels.HomeViewModel

    <h1 class="jumbotron">@Model.Title</h1>

    @foreach (var pie in Model.Pies)
    {
        <div class="list-group">
            <h2>@pie.Name</h2>
            <h2>@pie.Price.ToString("c")</h2>
        </div>
    }


